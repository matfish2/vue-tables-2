
// if (withVuex()) {
//     describe(suite + 'Vuex', () => {
//         it.only('allows for keeping an existing store when the table is reinitalized', (done) => {
//             createWrapper({
//                 preserveState:true
//              });

//             vm().setPage(2);
//             expect(vm().$store.state[source].page).toEqual(2);
//             vm().$destroy();
//             createWrapper({
//                preserveState:true
//             });


            
//             run(()=>{
//                 // expect(vm().$store.state[source].page).toEqual(2);                
//             }, done);
            
//         });
//     });
// }
